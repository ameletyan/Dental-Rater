{
  "name": "rating",
  "version": "1.0.0",
  "description": "An application that allows users to rate a service through email.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:db": "mysql -u root -p < database_init.sql",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.1",
    "express": "^4.13.4",
    "mysql": "^2.10.2",
    "nodemailer": "^2.3.2",
    "sequelize": "^3.23.0",
    "sequelize-cli": "^2.4.0"
  }
}
